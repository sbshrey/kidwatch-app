rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for device-id based linking mode.
    // Any device can read/write all documents without Firebase Auth.
    match /{document=**} {
      allow read, write: if true;
    }

  }
}
